# AnimeMaster Engine 2.4.1 专业评价报告

评估日期：2026-09-04

评估对象：AnimeMaster Engine 2.4.1 / Android build 2046

## 一、执行结论

AnimeMaster Engine 已具备个人长期维护项目所需的基本工程基线。核心浏览、追番、资源检索、BitTorrent 下载、在线播放、弹幕、Bangumi 授权与应用更新形成了完整产品闭环；Android 版本可以生成正式签名的分架构与通用安装包。

当前版本的主要优势是功能完整、异常降级较充分、客户端与授权网关职责清晰。2.4.1 已建立更新包完整性验证、OAuth 回调约束、远程源信任边界、下载目录删除边界、严格静态检查和持续集成门禁。项目已从“功能可用”进入“可持续维护”的阶段。

主要剩余风险集中在自动化测试覆盖率偏低、部分业务文件体积过大、第三方数据源稳定性不可控，以及 iOS/Windows 仍需在对应原生环境中持续验证。综合评价为：适合作为个人维护和小范围发布项目；在扩大用户规模前，应优先继续提高测试覆盖率并拆分高复杂度模块。

## 二、综合评分

| 维度 | 评分 | 评价 |
|---|---:|---|
| 产品完整度 | 8.5/10 | 主要使用链路完整，Android 交付能力成熟 |
| 安全性 | 8.0/10 | 关键高风险边界已有主动验证和服务端限制 |
| 代码质量 | 8.0/10 | 静态检查零问题，格式和异步调用规则统一 |
| 架构与可维护性 | 7.0/10 | 分层基础良好，但少数核心文件职责过重 |
| 测试与质量保障 | 6.5/10 | 104 项 Flutter 测试和 9 项 Worker 测试通过，行覆盖率仍仅 17.54% |
| 跨平台成熟度 | 6.5/10 | Android 已验证；iOS、Windows 构建已纳入 CI，但仍属实验支持 |
| 文档与合规 | 8.0/10 | 发布、安全、隐私、内容边界和许可说明齐全 |
| 综合评价 | 7.6/10 | 可交付、可维护，适合继续迭代 |

## 三、工程与架构评价

项目采用 Flutter 业务层、原生 FFI 核心、平台宿主和独立 OAuth 网关的组合架构。API、Repository、ViewModel、Provider、Service 与 Screen 已形成基本分层，网络失败时也具备缓存或备用路径，符合个人项目控制复杂度和快速迭代的需要。

原生核心对 Android、iOS 和 Windows 使用统一 C++ 实现，版本信息已经在 Flutter、CMake、Podspec 和原生接口间保持一致。Windows FFI 插件具有独立构建定义；未具备实现目录的平台不再对外声明支持，降低了错误的平台能力承诺。

当前结构性短板是部分文件过大：播放器页面接近 4,000 行，在线源服务接近 1,800 行，设置页和下载管理器均超过 1,000 行。它们包含状态控制、业务规则、网络协调和界面渲染等多类职责，后续变更容易扩大回归面。

建议逐步拆分为以下稳定边界：

- 播放会话、播放器控制器、弹幕轨道和播放页组件。
- 在线源注册表、站点适配器、媒体探测器和结果聚合器。
- 下载调度、任务持久化、速度采样、文件生命周期和播放优先级。
- 设置数据模型、账号设置、播放设置、资源设置和更新设置。

## 四、安全评价

### 应用更新

Android 更新链路要求清单和 APK 使用 HTTPS。APK 下载完成后校验 SHA-256，并由原生层再次校验应用包名、versionCode、安装版本递增关系和签名证书；只有全部一致时才会打开系统安装器。下载大小限制为 300 MB，超过限制会中止下载。发布脚本同时验证四个 APK 的包名、版本号、签名和哈希。

此设计能够抵御更新地址被替换、下载内容损坏、错误应用包、版本降级和非同签名安装包。发布身份锚点独立保存在 `release/release_identity.json`。

### OAuth 与网关

Bangumi 移动授权仅接受固定的 `animemasteroauth://callback` 回调，客户端会核对授权请求 ID。Worker 对授权开始、会话交换和 Bangumi 代理设置速率限制，浏览器跨域来源采用允许列表；预期的参数错误返回 400，不再作为服务器故障暴露。

OAuth 客户端密钥保留在服务端环境变量中，令牌和本地敏感凭据使用系统安全存储。个人本机网关的数据文件使用仅当前用户可读写的权限模式。

### 文件与远程来源

下载任务仅允许删除应用专用根目录中的子目录。删除前必须成功停止下载任务；路径越界、符号链接逃逸、停止超时或文件删除失败时均保留任务记录并向界面报告失败。BitTorrent info hash 必须为 40 位十六进制字符串。

远程在线源仅接受公网 HTTPS 地址，拒绝 localhost、环回地址、私有 IPv4、链路本地地址、组播地址、非法数字地址和 IPv6 字面量。该限制同时存在于 Flutter 客户端和 Worker 服务端。

## 五、质量保障评价

当前质量门禁包括：

- Dart 格式一致性检查。
- Flutter Analyzer 严格检查，当前为零错误、零警告、零提示。
- 104 项 Flutter 自动化测试。
- 9 项 Worker 自动化测试和两个 Node.js 语法检查。
- 17.50% 最低行覆盖率门禁；当前实测 17.54%（736/4195）。
- Android Debug 构建与正式 Release 构建。
- Android 四种 APK 的版本、包名、签名和 SHA-256 验证。
- GitHub Actions 中的 Android、Windows 和 iOS 构建任务。

覆盖率门禁只能视为防止继续下降的起点。下一阶段应优先覆盖更新下载失败、签名拒绝、任务恢复/暂停/删除、OAuth 状态机、在线源并发聚合和播放器状态切换。建议短期目标提高到 25%，中期提高到 40%，并对安全边界维持接近 100% 的分支覆盖。

## 六、平台与发布评价

Android 2.4.1+2046 已生成并验证以下正式包：

| 架构 | 文件 | 大小 | SHA-256 |
|---|---|---:|---|
| arm64-v8a | `app-arm64-v8a-release.apk` | 35.26 MB | `f9fb15aec567455453f4ad0a5ef2fa4cabb259b5cf0c236d840b81c9f4688005` |
| armeabi-v7a | `app-armeabi-v7a-release.apk` | 31.89 MB | `c6a1cb51b29156aad769bb27a4d10ed0bbfb43f6f19e8d749ca718998cfca06f` |
| x86_64 | `app-x86_64-release.apk` | 39.92 MB | `5a0b59fb73f156e55a1f6003fd61a4ff8bd91a11b22263347695690c841e0280` |
| 通用 | `app-release.apk` | 103.31 MB | `f342a45a26355fea7acdc0603cd90774a6bf3328b3224f9d1c8793dbd4bd6638` |

四个安装包均为 package `com.animemaster.app`、versionCode `2046`，签名证书 SHA-256 为 `aab3fdce4454a3557d2b0f63aef477255fb4f51e17dbfc70dab7c88560817fe6`。

iOS 已包含 OAuth URL Scheme 和相册权限用途说明；Windows 已包含 FFI 动态库构建定义。两者应以 GitHub Actions 对应系统的实际构建结果作为发布前准入条件。

## 七、文档、隐私与许可

项目采用个人维护的 source-available、保留全部权利许可，不属于开源许可证。第三方依赖继续遵循各自许可证。隐私说明明确了本地存储、系统安全存储、授权网关会话、第三方网络请求和用户数据控制方式；内容说明明确项目不托管或授权第三方媒体内容，并提示 BitTorrent 上传行为和地区法律差异。

发布前仍应根据实际分发地区和所使用的第三方服务条款复核合规要求。若未来接受外部贡献或公开二次分发，需要另行选择明确的开源或贡献者许可方案。

## 八、剩余风险与优先级

### P1：下一个维护版本

- 将测试覆盖率稳定提高到 25% 以上，重点覆盖更新、下载、授权和播放状态机。
- 拆分播放器、在线源和下载管理器，降低单文件复杂度。
- 在真实 iOS 和 Windows 环境完成首次 CI 构建闭环并修正平台差异。

### P2：中期治理

- 建立第三方在线源的健康度、失败率和自动隔离策略。
- 为依赖升级建立季度窗口，逐批处理当前受版本约束的依赖更新。
- 将发布 APK 统一交由 Git LFS 或 GitHub Releases 管理，避免普通 Git 对象继续增长。
- 为发布清单增加离线签名；SHA-256 能验证文件完整性，但清单签名可进一步降低分发端被整体替换的风险。

### P3：规模扩大前

- 增加崩溃和性能监测，并确保符合隐私说明和用户授权要求。
- 对授权网关执行并发限流、令牌生命周期和故障恢复压力测试。
- 建立第三方内容投诉、恶意来源下线和安全响应流程。

## 九、最终意见

AnimeMaster Engine 2.4.1 达到个人维护项目的正式可交付标准。Android 主路径具备完整构建与升级安全保障，工程质量门禁可以阻止常见回归，项目边界和法律说明也更加明确。继续投入时，最高收益方向不是增加更多功能，而是提高关键路径测试覆盖率并拆分高复杂度模块；完成这两项后，项目可维护性和跨平台发布可信度会显著提升。
