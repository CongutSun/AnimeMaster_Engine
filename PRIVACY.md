# AnimeMaster 隐私说明

AnimeMaster 是个人维护的客户端项目，不内置广告、用户画像或第三方分析 SDK。

## 本地保存的数据

- Bangumi 用户名、头像地址、界面设置、RSS 配置和播放进度保存在应用本地。
- Bangumi 访问令牌、授权会话标识和弹弹play AppSecret 保存在操作系统提供的安全存储中。
- 下载任务、种子元数据和媒体缓存保存在应用专用目录中。

## 网络请求

根据用户使用的功能，应用会连接 Bangumi、AnimeMaster 授权网关、弹弹play、Animeko、RSS 提供方、Tracker、在线媒体来源和 GitHub Releases。搜索词、番剧标识、IP 地址及必要的请求元数据可能由这些第三方服务处理，并受其各自隐私政策约束。

授权网关会临时保存 OAuth 状态、访问令牌和刷新令牌，以完成登录和令牌刷新。会话默认最长保留 60 天，退出账号时应用会请求删除服务端会话。

弹弹play 默认接入通过 AnimeMaster 网关转发，应用密钥仅保存在服务端。自动识别本地视频时会读取文件开头最多 16 MiB，在本机计算 MD5 指纹，并上传文件名、文件大小和指纹；不会上传视频内容。番名、作品与剧集编号用于搜索、映射和弹幕读取。手动匹配与弹幕缓存在本地保存，清除应用数据可删除。

发送弹幕需要用户主动确认。发送内容、剧集、视频时间点及由随机安装标识派生的匿名名称会交给弹弹play，并对本应用其他用户可见。网关最多保留 31 天的发送结果与去重标识以防止重复发送；读取缓存最多保留一天。IP 地址的哈希值用于一分钟频率限制，过期记录按小时清理。发送额度由整个应用共享，匿名标识不用于广告或跨应用追踪。

## 用户控制

用户可以在应用中退出 Bangumi、删除下载任务、清除应用数据或卸载应用。卸载前如需保留下载内容，请自行备份。

## 未成年人

本项目不以收集未成年人信息为目的。监护人应根据所在地规则决定是否允许未成年人使用第三方内容和下载功能。
