# AnimeMaster 维护路线图

## 当前基线：2.4.1

2.4.1 建立个人长期维护所需的安全和工程基线：OAuth 回调绑定、更新包自动校验、下载目录边界、严格静态检查、关键安全测试、统一版本信息，以及隐私和内容使用说明。

Android 是正式支持平台；iOS 与 Windows 当前为实验性支持。

## 下一阶段：播放器模块化

- 将 `VideoPlayerPage` 拆分为播放会话、控制层、手势层、弹幕控制器和选集导航。
- 合并 `VideoPlayerPage` 与 `EpisodeWatchPage` 的重复播放状态和生命周期处理。
- 为续播、自动下一集、切源、全屏和画中画建立状态机测试。
- 播放错误区分网络、来源失效、格式不支持和本地文件损坏，并提供对应恢复动作。

## 下一阶段：下载可靠性

- 为任务恢复、暂停、删除、并发限制和边下边播 Range 请求建立集成测试。
- 下载完成、失败和长时间无速度时提供系统通知。
- 明确做种开关、上传状态和流量说明。
- 为数据库升级增加显式迁移策略和备份验证。

## 下一阶段：来源治理

- 将在线来源拆分为独立适配器文件和可测试注册表。
- 远程来源清单增加签名、版本号、回滚保护和域名撤销列表。
- 记录来源健康状态、失败原因和最近成功时间。
- 对第三方代理和网关增加部署层速率限制、告警和成本上限。

## 下一阶段：平台与发布

- 在 macOS 环境验证 iOS 构建、OAuth 回调、相册选择和播放器。
- 在 Windows CI 中验证 FFI 动态库、媒体播放和安全存储。
- 发布流程自动生成更新清单、发布说明和哈希，并验证 GitHub Release 资产存在。
- 源码仓库不再新增 APK 二进制，发布产物统一交由 GitHub Releases 保存。

## 长期质量目标

| 领域 | 目标 |
|---|---|
| 静态分析 | 0 error、0 warning、0 lint info |
| 核心服务覆盖率 | 不低于 60% |
| 全项目有效覆盖率 | 先达到 35%，再逐步提升至 50% |
| Android | 每次提交执行测试与 Debug 构建 |
| OAuth / 更新 / 文件删除 | 每次安全相关修改必须包含回归测试 |
| 单文件规模 | 新增业务文件原则上不超过 800 行 |
