# Run with `dart run ffigen --config ffigen.yaml`.
name: AnimemasterBindings
description: |
  Bindings for `src/animemaster.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/animemaster_bindings_generated.dart'
headers:
  entry-points:
    - 'src/animemaster.h'
  include-directives:
    - 'src/animemaster.h'
comments:
  style: any
  length: full
